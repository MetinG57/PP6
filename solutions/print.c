#include <stdio.h>

int main(void) {
    printf("Hallo aus C!\n");  // Begrüßung ausgeben
    printf("Zahl: %d, Komma: %.2f, Text: %s\n", 42, 3.14, "test");  // Formatierten Output
    puts("Das kommt von puts");  // Einfache Ausgabe mit puts
    return 0;  // Programm beenden
}
